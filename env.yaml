conda create -n phenocopy python=3.9 scipy
conda activate phenocopy
conda install -c conda-forge lifelines
conda install ipykernel
conda deactivate
#python -m ipykernel install --user --name=phenocopy
ipython kernel install --name phenocopy --user
conda activate phenocopy
pip install tqdm
pip install -U scikit-learn
#pip install pyupset
pip install openpyxl
pip install biomart
pip install jupyterlab
pip install seaborn
pip install umap-learn
pip install networkx
#pip install patchworklib
#conda install --channel conda-forge --channel bioconda bedtools htslib
